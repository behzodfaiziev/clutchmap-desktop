import 'package:flutter/material.dart';

/// ui_stitch design tokens: primary #f47b25, dark backgrounds.
class AppColors {
  static const primary = Color(0xFFF47B25);
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);

  static const aggression = Color(0xFFEF4444);
  static const structure = Color(0xFF3B82F6);
  static const risk = Color(0xFFF97316);
  static const variety = Color(0xFF8B5CF6);

  static const surfaceDark = Color(0xFF1E1E1E);
  static const surfaceLight = Color(0xFFFFFFFF);

  /// ui_stitch: scaffold / page background dark.
  static const backgroundDark = Color(0xFF0A0A0A);
  /// ui_stitch: card / surface dark (neutral-surface).
  static const neutralSurface = Color(0xFF1A1A1A);
  /// ui_stitch: border (neutral-border).
  static const neutralBorder = Color(0xFF2A2A2A);
  /// Sidebar / nav background (neutral-900).
  static const neutral900 = Color(0xFF141210);
}


